# Build a partir da RAIZ do repo (contexto = .). Debian (slim) em vez de Alpine
# porque o engine do Prisma precisa de OpenSSL/glibc compatíveis.
FROM node:20-slim
RUN apt-get update \
  && apt-get install -y --no-install-recommends openssl ca-certificates \
  && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY tsconfig.base.json ./tsconfig.base.json
# docs/ vive na raiz do monorepo — necessário para o Swagger (GET /docs) achar
# o WhatFlow_openapi.yaml (core/swagger.ts procura em ../../docs relativo ao cwd).
COPY docs/ ./docs/
WORKDIR /app/apps/api
COPY apps/api/package.json ./package.json
RUN npm install
COPY apps/api/ ./
RUN npm run prisma:generate
EXPOSE 3000
CMD ["npm", "run", "dev"]
